# Backend URL
# Leave empty for local development — Vite proxy forwards /api, /upload, /verify to localhost:3000
# In production, set this to your Render backend URL (e.g. https://your-app.onrender.com)
VITE_API_URL=
